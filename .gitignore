.DS_Store
__pycache__/
*.pyc

# viewer: generated locally, contains private investigation content — never commit
viewer/index.json
viewer/.run/

# test deps (one node package at the repo root serves tests/ and viewer/tests/)
/node_modules/
/package-lock.json
